[tool.ruff.lint]
# Явная политика не позволяет обновлению Ruff молча включить новые семейства правил.
select = ["E4", "E7", "E9", "F"]

[tool.ruff.lint.per-file-ignores]
# main.py: импорты специально расположены ниже load_dotenv() для загрузки .env
"main.py" = ["E402"]
